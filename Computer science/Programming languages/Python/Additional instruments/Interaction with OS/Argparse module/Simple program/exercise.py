# Create an argument parser using the argparse module, and 
# name it parser. Then, add an argument --number.

# Your program should NOT print anything.

import argparse

parser = argparse.ArgumentParser()
parser.add_argument("--number")