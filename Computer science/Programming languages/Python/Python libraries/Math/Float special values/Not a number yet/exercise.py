# Imagine you have decided to replace all NaNs in your data with the median value. In
# order to do so, you need to detect NaN values first.

# Test a value for NaN in the variable check, please.

import math

value = float(input())
# change the next line
check = math.isnan(value)