# In the string module, there are constants digits and
# ascii_lowercase. Print digits in the first line of the output,
# and ascii_lowercase in the second line.
from string import digits
from string import ascii_lowercase
print(digits)
print(ascii_lowercase)